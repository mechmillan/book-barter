const kittenRouter = require('express').Router();

// matches GET requests to /api/kittens
kittenRouter.get('/', (req, res, next) => {

});

// matches POST requests to /api/kittens
kittenRouter.post('/', (req, res, next) => {

});

// matches PUT requests to /api/kittens/:kittenId
kittenRouter.put('/:kittenId', (req, res, next) => {

});

// matches DELETE requests to /api/kittens/:kittenId
kittenRouter.delete('/:kittenId', (req, res, next) => {

});

// module.exports = kittenRouter;
